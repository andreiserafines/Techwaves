ScrollReveal().reveal(".reveal", {
  distance: "50px",
  duration: 1000,
  easing: "ease-in-out",
  origin: "bottom",
  interval: 200,
});

ScrollReveal().reveal(".reveal-text", {
  distance: "20px",
  duration: 1000,
  easing: "ease-in-out",
  origin: "bottom",
  delay: 100, // delay for text elements
  interval: 200,
});
